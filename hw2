#Önce kullanıcıdan bir sayı alıyoruz.
sayi= int(input("Bir sayı giriniz: "))

#Girilen sayının negatif olmaması için
if sayi < 0:
    print ("Negatif bir sayı girdiniz!")
else:

 #Sayının tek mi çift mi olduğunu gösteriyoruz.
    if sayi %2 == 0:
        print (f"{sayi} bir çift sayıdır!")
    else:
       print(f"{sayi} bir tek sayıdır!")
